# dependencies
node_modules/

# live data (customer bookings, names, phone numbers) — keep OUT of git
data/

# local off-site backups (full guest PII dumps) — keep OUT of git
backups/

# logs & env
# *.log
# npm-debug.log*
# .env
# .env.*

# Firebase service-account credentials — NEVER commit these

# OS / editor cruft
Thumbs.db
.DS_Store
.vscode/

# local tool settings
.claude/settings.local.json

# Firebase service account secret — never commit
serviceAccountKey.json

# one-line copy of the service-account key for pasting into a host env var — NEVER commit
serviceAccountKey.oneline.txt
