<!DOCTYPE html>
<html lang="en">
<head>
<%- include('partials/seed') %>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staycation Haven PH</title>
    <link rel="icon" href="images/logo.png">
    <link rel="stylesheet" href="style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600..900&family=Inter:wght@300;400;500;600;700&family=Manrope:wght@400;500;600;700;800&family=Cormorant+Garamond:wght@600;700&display=swap" rel="stylesheet">
</head>
<body>

<!-- ===== Logo Splash intro (imported from Claude Design) ===== -->
<div id="sgSplash" aria-hidden="true">
  <div class="sg-wrap">
    <div class="sg-glow"></div>
    <div class="sg-ring"></div>
    <img class="sg-logo" src="images/logo.png" alt="Staycation Haven PH" onerror="this.style.display='none'">
    <div class="sg-name">Staycation Haven PH</div>
    <div class="sg-bar"><span></span></div>
  </div>
</div>
<style>
  @keyframes sgLogoIn { 0%{opacity:0;transform:scale(.78);filter:blur(6px)} 55%{opacity:1;filter:blur(0)} 100%{opacity:1;transform:scale(1);filter:blur(0)} }
  @keyframes sgGlow { 0%{opacity:0;transform:translate(-50%,-50%) scale(.7)} 45%{opacity:.9} 100%{opacity:.55;transform:translate(-50%,-50%) scale(1)} }
  @keyframes sgRing { 0%{opacity:0;transform:translate(-50%,-50%) scale(.55)} 35%{opacity:.5} 100%{opacity:0;transform:translate(-50%,-50%) scale(1.75)} }
  @keyframes sgBar { 0%{transform:scaleX(0)} 100%{transform:scaleX(1)} }
  @keyframes sgOut { 0%{opacity:1;transform:scale(1);filter:blur(0)} 100%{opacity:0;transform:scale(1.09);filter:blur(6px)} }
  #sgSplash{ position:fixed; inset:0; z-index:99999; display:flex; align-items:center; justify-content:center;
    background:radial-gradient(circle at 50% 44%, #1b1b1e 0%, #0d0d0f 70%); font-family:'Inter',system-ui,sans-serif; }
  #sgSplash.sg-hide{ animation:sgOut .8s cubic-bezier(.5,0,.2,1) forwards; }
  #sgSplash .sg-wrap{ position:relative; display:flex; flex-direction:column; align-items:center; }
  #sgSplash .sg-glow{ position:absolute; top:50%; left:50%; width:440px; height:440px; margin-top:-20px;
    border-radius:50%; background:radial-gradient(circle, rgba(201,162,74,.34) 0%, rgba(201,162,74,.10) 38%, transparent 66%);
    animation:sgGlow 1s ease-out both; pointer-events:none; }
  #sgSplash .sg-ring{ position:absolute; top:50%; left:50%; width:250px; height:250px; margin-top:-20px;
    border-radius:50%; border:1px solid rgba(201,162,74,.5); animation:sgRing 1.6s ease-out .25s both; pointer-events:none; }
  #sgSplash .sg-logo{ position:relative; width:min(216px,46vw); height:auto; object-fit:contain;
    animation:sgLogoIn 1s cubic-bezier(.22,.61,.36,1) .12s both; filter:drop-shadow(0 6px 26px rgba(201,162,74,.35)); }
  #sgSplash .sg-name{ position:relative; margin-top:24px; font-family:'Cormorant Garamond',serif; font-size:clamp(20px,5vw,30px);
    letter-spacing:.16em; color:#e7c877; text-transform:uppercase; line-height:1; text-align:center;
    animation:sgLogoIn 1s cubic-bezier(.22,.61,.36,1) .42s both; }
  #sgSplash .sg-bar{ margin-top:32px; width:132px; height:2px; border-radius:2px; background:rgba(255,255,255,.10); overflow:hidden; }
  #sgSplash .sg-bar span{ display:block; height:100%; width:100%; transform-origin:left; transform:scaleX(0);
    background:linear-gradient(90deg,#c9a24a,#e7c877); animation:sgBar 2.1s cubic-bezier(.4,0,.2,1) .2s both; }
  @media (prefers-reduced-motion: reduce){ #sgSplash *{ animation-duration:1ms !important; } }
</style>
<script>
(function(){
  var el = document.getElementById("sgSplash");
  if(!el) return;
  var root = document.documentElement;

  // play once per browser session — navigating back to the homepage won't replay it
  try{ if(sessionStorage.getItem("shph_splash_seen") === "1"){ el.remove(); return; } sessionStorage.setItem("shph_splash_seen", "1"); }catch(e){}
  root.style.overflow = "hidden";   // no scroll during the intro

  // --- key out the logo's white background (same-origin canvas → no taint) ---
  var logo = el.querySelector(".sg-logo"), keyed = false;
  function keyWhite(){
    if(keyed || !logo || !logo.naturalWidth) return; keyed = true;
    try{
      var c = document.createElement("canvas");
      c.width = logo.naturalWidth; c.height = logo.naturalHeight;
      var ctx = c.getContext("2d");
      ctx.drawImage(logo, 0, 0);
      var im = ctx.getImageData(0, 0, c.width, c.height), p = im.data;
      for(var i = 0; i < p.length; i += 4){
        var mn = Math.min(p[i], p[i + 1], p[i + 2]);   // whiteness ≈ the smallest channel
        if(mn > 210) p[i + 3] = 0;                       // near-white → transparent
        else if(mn > 170) p[i + 3] = Math.round((210 - mn) / 40 * 255);   // soft edge feather
      }
      ctx.putImageData(im, 0, 0);
      logo.src = c.toDataURL("image/png");
    }catch(e){}
  }
  if(logo){ if(logo.complete && logo.naturalWidth) keyWhite(); else logo.addEventListener("load", keyWhite); }

  // --- timing ---
  setTimeout(function(){ el.classList.add("sg-hide"); }, 2150);    // begin fade-out
  setTimeout(function(){ el.remove(); root.style.overflow = ""; }, 3000);  // remove after fade
})();
</script>
<!-- ===== /Logo Splash ===== -->

<!-- ============================================================
     Front page — imported from Claude Design "Staycation Haven PH"
     Scoped styles (sh-*) live here so other pages (which share
     style.css) are unaffected. The booking search, date picker,
     guest selector and live haven cards keep their original hooks.
     ============================================================ -->
<style>
  /* self-hosted Recoleta (the design's headline font) — single Black weight file */
  @font-face{
    font-family:'Recoleta';
    src:url('/fonts/RecoletaAlt-Bold.ttf') format('truetype');
    font-weight:400 900; font-style:normal; font-display:swap;
  }
  :root{
    --sh-bg:#faf6ef; --sh-card:#ffffff; --sh-ink:#1c1a17; --sh-muted:#6a6459;
    --sh-gold:#a9842b; --sh-gold-2:#c99a3f; --sh-line:#ece5d7; --sh-footer:#f2ece0;
  }
  /* headline serif — Recoleta (Fraunces is the fallback if the font can't load) */
  .sh-brand, .sh-badge, .sh-hero h1, #results h2, .sh-sec-head h2, .sh-card-head h3, .sh-promo h2{ font-family:'Recoleta', 'Fraunces', Georgia, 'Times New Roman', serif; }
  .sh-hero h1, .sh-promo h2, .sh-sec-head h2{ font-weight:800; }
  *, *::before, *::after{ box-sizing:border-box; }
  body{ background:var(--sh-bg); color:var(--sh-ink); font-family:'Inter',system-ui,-apple-system,sans-serif; margin:0; }
  .sh-wrap{ max-width:1400px; margin:0 auto; padding-left:clamp(20px,4vw,56px); padding-right:clamp(20px,4vw,56px); }

  /* nav — overrides the bare header{} rule in style.css (padding/white bg/shadow) */
  .sh-nav{ position:sticky; top:0; z-index:60; display:block; padding:0; box-shadow:none; background:rgba(250,246,239,.9); backdrop-filter:blur(8px); border-bottom:1px solid var(--sh-line); }
  /* header spans the FULL screen width — logo pinned left, menu pinned right (content stays centered) */
  .sh-nav-in{ max-width:none; margin:0; padding:15px clamp(20px,4vw,48px); display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap; }
  .sh-brand{ display:flex; align-items:center; gap:10px; text-decoration:none; color:var(--sh-ink); font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:22px; letter-spacing:.01em; }
  .sh-badge{ width:40px; height:40px; border-radius:10px; background:#fff; display:grid; place-items:center; overflow:hidden; box-shadow:0 3px 10px rgba(156,115,39,.22); border:1px solid var(--sh-line); flex:none; }
  .sh-badge img{ width:100%; height:100%; object-fit:contain; padding:3px; }
  .sh-links{ display:flex; gap:28px; }
  .sh-links a{ margin:0; text-decoration:none; color:var(--sh-ink); font-size:14px; font-weight:400; }
  .sh-links a:hover{ color:var(--sh-gold); }
  .sh-burger{ display:none; flex-direction:column; align-items:center; justify-content:center; gap:4px; width:44px; height:44px; border-radius:12px; background:#fff; border:1px solid var(--sh-line); cursor:pointer; flex:none; padding:0; }
  .sh-burger span{ display:block; width:20px; height:2px; border-radius:2px; background:var(--sh-ink); }
  .sh-burger:hover{ background:#f6f1e7; }

  /* hero — matches the design: 1fr 1.05fr, 56px gap, copy vertically centered */
  .sh-hero{ position:relative; padding-top:64px; padding-bottom:72px; }
  .sh-hero-grid{ display:grid; grid-template-columns:1fr 1.05fr; gap:56px; align-items:stretch; }
  .sh-hero-copy{ display:flex; flex-direction:column; justify-content:center; }
  .sh-eyebrow{ color:var(--sh-gold); font-size:13px; font-weight:600; letter-spacing:.28em; text-transform:uppercase; margin-bottom:24px; }
  .sh-hero h1{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:clamp(40px,6vw,72px); line-height:1.02; letter-spacing:-.01em; margin:0 0 22px; }
  .sh-lead{ color:var(--sh-muted); font-size:18px; line-height:1.65; max-width:420px; margin:0 0 38px; }

  /* search card (native date inputs + working #searchHours / guest hooks) */
  .sh-search{ background:#fff; border:1px solid var(--sh-line); border-radius:20px; box-shadow:0 24px 50px rgba(80,60,20,.20); padding:12px; max-width:620px; position:relative; }
  .sh-search-grid{ display:grid; grid-template-columns:1fr 1fr; gap:2px; }
  .sh-search .search-item{ padding:14px 20px; text-align:left; position:relative; }
  .sh-search .search-item:nth-child(2){ border-left:1px solid #efe9dd; }
  .sh-search .search-item:nth-child(3){ border-top:1px solid #efe9dd; }
  .sh-search .search-item:nth-child(4){ border-top:1px solid #efe9dd; border-left:1px solid #efe9dd; }
  .sh-search .label{ display:block; font-size:11px; letter-spacing:.12em; color:#a09884; text-transform:uppercase; margin-bottom:5px; font-weight:700; }
  .sh-search .value{ font-size:16px; color:#2a251d; font-weight:400; }
  .sh-search .sh-date{ font-family:inherit; font-size:16px; color:#2a251d; border:none; background:none; padding:0; margin:0; width:100%; cursor:pointer; outline:none; }
  .sh-search .sh-date::-webkit-calendar-picker-indicator{ opacity:.55; cursor:pointer; }
  .sh-search #searchHours{ border:none; background:none; padding:0; margin:0; font-family:inherit; font-size:16px; color:#2a251d; width:100%; cursor:pointer; outline:none; appearance:none; }
  .sh-search .guest-trigger{ cursor:pointer; }
  .sh-search .guest-trigger .value{ display:block; }
  .sh-cta{ display:block; width:100%; margin-top:10px; height:58px; padding:0; border:none; border-radius:12px; background:var(--sh-ink); color:#f0d488; font-family:'Manrope','Inter',sans-serif; font-weight:700; font-size:16px; letter-spacing:.02em; cursor:pointer; }
  .sh-cta:hover{ background:#000; }
  /* clickable date fields + custom range calendar */
  .sh-datefield, .sh-hours-field{ cursor:pointer; }
  .sh-search .sh-dateval{ display:block; }
  .sh-search .sh-dateval.ph{ color:#b4ac9c; }
  .sh-cal{ position:absolute; top:calc(100% + 10px); left:0; width:322px; max-width:calc(100vw - 32px); background:#fff; border:1px solid var(--sh-line); border-radius:18px; box-shadow:0 22px 50px rgba(80,60,20,.20); padding:14px 16px 12px; z-index:80; }
  .sh-cal-head{ display:flex; align-items:center; justify-content:space-between; margin-bottom:10px; }
  .sh-cal-head > span{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:17px; }
  .sh-cal-nav{ border:none; background:#f6f1e7; width:32px; height:32px; border-radius:9px; cursor:pointer; font-size:18px; line-height:1; color:var(--sh-ink); }
  .sh-cal-nav:hover{ background:#efe7d6; }
  .sh-cal-nav:disabled{ opacity:.35; cursor:default; }
  .sh-cal-dow{ display:grid; grid-template-columns:repeat(7,1fr); margin-bottom:2px; }
  .sh-cal-dow span{ text-align:center; font-size:11px; color:#a09884; font-weight:700; padding:4px 0; }
  .sh-cal-grid{ display:grid; grid-template-columns:repeat(7,1fr); gap:2px; }
  .sh-cal-grid button, .sh-cal-grid .empty, .sh-cal-grid .past{ height:38px; width:100%; display:flex; align-items:center; justify-content:center; border-radius:9px; font-size:13.5px; font-family:inherit; }
  .sh-cal-grid button{ border:none; background:none; cursor:pointer; color:var(--sh-ink); padding:0; }
  .sh-cal-grid button:hover{ background:#f6f1e7; }
  .sh-cal-grid button.inrange{ background:#f3e8cf; border-radius:0; }
  .sh-cal-grid button.sel{ background:var(--sh-ink); color:#fff; font-weight:700; }
  .sh-cal-grid .empty, .sh-cal-grid .past{ color:#d3cabb; }
  .sh-cal-foot{ display:flex; align-items:center; justify-content:space-between; gap:10px; margin-top:10px; padding-top:10px; border-top:1px solid var(--sh-line); }
  .sh-cal-foot > span{ font-size:12.5px; color:var(--sh-muted); }
  .sh-cal-foot button{ border:none; background:none; color:var(--sh-gold); font-weight:600; font-size:13px; cursor:pointer; }
  /* custom hours dropdown (rounded panel like the guests popup) */
  .sh-hours-field{ position:relative; }
  .sh-hours-pop{ position:absolute; top:calc(100% + 10px); left:8px; min-width:190px; background:#fff; border:1px solid var(--sh-line); border-radius:16px; box-shadow:0 16px 40px rgba(80,60,20,.16); padding:6px; z-index:70; }
  .sh-hours-opt{ padding:11px 14px; border-radius:11px; font-size:15px; color:var(--sh-ink); cursor:pointer; }
  .sh-hours-opt:hover{ background:#f6f1e7; }
  .sh-hours-opt.is-on{ background:var(--sh-ink); color:#fff; font-weight:600; }

  /* hero photo + overlay tag (min-height 560 like the design) */
  .sh-hero-media{ position:relative; border-radius:22px; overflow:hidden; box-shadow:0 24px 50px rgba(60,45,20,.16); min-height:560px; background:#e7ded0; }
  .sh-hero-media img{ position:absolute; inset:0; width:100%; height:100%; object-fit:cover; display:block; }
  .sh-hero-tag{ position:absolute; left:22px; bottom:22px; background:rgba(255,255,255,.94); backdrop-filter:blur(8px); border-radius:14px; padding:14px 18px; text-decoration:none; }
  .sh-hero-tag b{ display:block; font-family:'Recoleta','Fraunces',Georgia,serif; color:var(--sh-ink); font-size:18px; font-weight:700; }
  .sh-hero-tag span{ display:block; color:var(--sh-gold); font-size:14px; font-weight:600; margin-top:2px; }

  /* results (availability search output) */
  #results{ max-width:1400px; margin:0 auto; padding:8px 24px 0; }
  #results:empty{ display:none; padding:0; }
  #results h2{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:30px; margin:14px 0 4px; }

  /* section header */
  .sh-section{ padding-top:40px; padding-bottom:80px; }
  .sh-sec-head{ display:flex; align-items:flex-end; justify-content:space-between; gap:16px; margin-bottom:32px; }
  .sh-sec-eyebrow{ color:var(--sh-gold); font-size:13px; font-weight:600; letter-spacing:.24em; text-transform:uppercase; margin-bottom:10px; }
  .sh-sec-head h2{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:44px; letter-spacing:-.01em; margin:0; }
  .sh-viewall{ color:var(--sh-gold); text-decoration:none; font-size:15px; font-weight:600; white-space:nowrap; }
  .sh-viewall:hover{ text-decoration:underline; }

  /* haven cards */
  .sh-cards{ display:grid; grid-template-columns:repeat(3,1fr); gap:28px; }
  .sh-card{ background:#fff; border:1px solid var(--sh-line); border-radius:20px; overflow:hidden; box-shadow:0 20px 44px rgba(80,60,20,.16); display:flex; flex-direction:column; transition:transform .2s ease, box-shadow .2s ease; }
  .sh-card:hover{ transform:translateY(-6px); box-shadow:0 28px 54px rgba(80,60,20,.24); }
  .sh-card:hover .sh-card-media img{ transform:scale(1.04); }
  .sh-card-media{ display:block; overflow:hidden; }
  .sh-card-media img{ width:100%; height:260px; object-fit:cover; display:block; transition:transform .35s ease; }
  .sh-card-body{ padding:22px 24px 26px; display:flex; flex-direction:column; flex:1; }
  .sh-card-head{ display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:6px; }
  .sh-card-head h3{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; font-size:22px; margin:0; }
  .sh-card-meta{ color:#8a8377; font-size:13px; font-weight:600; white-space:nowrap; }
  .sh-card-desc{ color:var(--sh-muted); font-size:14px; line-height:1.55; margin:0 0 16px; }
  .sh-card-foot{ display:flex; align-items:center; justify-content:space-between; margin-top:auto; gap:10px; }
  .sh-card-price{ color:var(--sh-gold); font-size:15px; font-weight:700; }
  .sh-book{ background:var(--sh-ink); color:#f0d488; text-decoration:none; padding:9px 20px; border-radius:10px; font-family:'Manrope','Inter',sans-serif; font-size:14px; font-weight:700; }
  .sh-book:hover{ background:#000; }

  /* promo banner */
  .sh-promo{ padding-top:8px; padding-bottom:48px; }
  .sh-promo-in{ background:#161513; border-radius:20px; padding:40px 44px; display:flex; align-items:center; justify-content:space-between; gap:24px; flex-wrap:wrap; }
  .sh-promo-eyebrow{ color:var(--sh-gold-2); font-size:12px; letter-spacing:.24em; text-transform:uppercase; margin-bottom:12px; }
  .sh-promo h2{ font-family:'Recoleta','Fraunces',Georgia,serif; font-weight:700; color:#fff; font-size:clamp(24px,3vw,32px); line-height:1.18; margin:0; max-width:560px; }
  .sh-promo-btn{ background:linear-gradient(180deg,#dcb052,#c2932e); color:#241a08; text-decoration:none; padding:14px 24px; border-radius:11px; font-weight:600; font-size:14px; white-space:nowrap; box-shadow:0 6px 16px rgba(194,147,46,.35); }
  .sh-promo-btn:hover{ background:linear-gradient(180deg,#e3b959,#c99a34); }

  /* footer */
  .sh-footer{ background:var(--sh-footer); border-top:1px solid var(--sh-line); }
  .sh-footer-in{ max-width:1400px; margin:0 auto; padding:48px 24px 28px; display:grid; grid-template-columns:1.5fr 1fr 1fr; gap:32px; }
  .sh-foot-brand p{ color:var(--sh-muted); font-size:13px; line-height:1.6; max-width:290px; margin:12px 0 0; }
  .sh-foot-col h4{ color:#9a9384; font-size:11px; letter-spacing:.18em; text-transform:uppercase; margin:0 0 14px; font-weight:600; }
  .sh-foot-col a, .sh-foot-col span{ display:block; color:var(--sh-ink); text-decoration:none; font-size:14px; margin-bottom:10px; }
  .sh-foot-col a:hover{ color:var(--sh-gold); }
  .sh-foot-bar{ border-top:1px solid var(--sh-line); text-align:center; padding:16px; color:#9a9384; font-size:12.5px; }

  @media(max-width:900px){
    .sh-hero{ padding-top:20px; padding-bottom:26px; }
    .sh-hero-grid{ grid-template-columns:1fr; gap:18px; }
    /* photo on top, FIXED height (a fixed height can't collapse the way aspect-ratio can on iOS) */
    .sh-hero-media{ order:-1; height:clamp(200px,42vw,320px); min-height:0; }
    .sh-search{ max-width:none; }
    .sh-cards{ grid-template-columns:repeat(2,1fr); }
    .sh-footer-in{ grid-template-columns:1fr 1fr; }
    .sh-foot-brand{ grid-column:1 / -1; }
  }
  /* mobile header → hamburger menu (links become a dropdown) */
  @media(max-width:760px){
    .sh-burger{ display:flex; }
    .sh-links{ display:none; position:absolute; top:100%; right:clamp(16px,4vw,48px); margin-top:8px; flex-direction:column; gap:0; background:#fff; border:1px solid var(--sh-line); border-radius:14px; box-shadow:0 18px 40px rgba(80,60,20,.18); padding:8px; min-width:190px; z-index:75; }
    .sh-links.show{ display:flex; }
    .sh-links a{ padding:12px 14px; border-radius:10px; font-size:15px; }
    .sh-links a:hover{ background:#f6f1e7; color:var(--sh-ink); }
  }
  /* phone: tighten the hero so the photo + copy + search all fit on one screen */
  @media(max-width:640px){
    .sh-hero{ padding-top:14px; padding-bottom:22px; }
    .sh-hero-grid{ gap:16px; }
    .sh-hero-media{ height:clamp(180px,44vw,240px); }
    .sh-eyebrow{ font-size:11px; letter-spacing:.16em; margin-bottom:12px; }
    .sh-hero h1{ font-size:clamp(26px,7.4vw,36px); line-height:1.06; margin:0 0 12px; }
    .sh-lead{ font-size:15px; line-height:1.55; margin:0 0 18px; }
    .sh-search{ padding:8px; }
    .sh-search .search-item{ padding:12px 14px; }
    .sh-cta{ height:52px; }
    /* "Our Havens" → swipeable card carousel */
    .sh-cards{ display:flex; overflow-x:auto; gap:16px; scroll-snap-type:x mandatory; -webkit-overflow-scrolling:touch; padding:2px 2px 10px; }
    .sh-card{ flex:0 0 84%; scroll-snap-align:start; }
    .sh-promo-in{ padding:28px 22px; }
    .sh-sec-head h2{ font-size:30px; }
    .sh-footer-in{ grid-template-columns:1fr 1fr; }
  }
  @media(max-width:480px){
    .sh-search .guest-popup{ width:min(320px,84vw); left:0; right:auto; }
  }
  @media(max-width:430px){
    .sh-nav-in{ padding-left:16px; padding-right:16px; }
    .sh-brand{ font-size:18px; }
    .sh-footer-in{ grid-template-columns:1fr; }
  }
</style>

<header class="sh-nav">
  <div class="sh-nav-in">
    <a class="sh-brand" href="index.html"><span class="sh-badge"><img src="images/logo.png" alt="Staycation Haven PH"></span> Staycation Haven PH</a>
    <nav class="sh-links" id="shLinks">
      <a href="index.html">Home</a>
      <a href="havens.html">Havens</a>
      <a href="#promo">Promos</a>
      <a href="#contact">Contact</a>
    </nav>
    <button class="sh-burger" aria-label="Menu" onclick="shToggleNav(event)"><span></span><span></span><span></span></button>
  </div>
</header>

<section class="sh-hero sh-wrap">
  <div class="sh-hero-grid">

    <div class="sh-hero-copy">
      <div class="sh-eyebrow" id="shHeroEyebrow">Comfortable · Affordable · Memorable</div>
      <h1 id="shHeroTitle">Your quiet escape<br>above Panay Ave.</h1>
      <p class="sh-lead" id="shHeroLead">Thoughtfully styled units at Mplace Tower D, Quezon City — from ₱999 a night.</p>

      <div class="sh-search">
        <div class="sh-search-grid">
          <div class="search-item sh-datefield" onclick="shOpenCal('in', event)">
            <span class="label">Check-in</span>
            <span class="value sh-dateval ph" id="shCiLabel">dd/mm/yyyy</span>
          </div>
          <div class="search-item sh-datefield" onclick="shOpenCal('out', event)">
            <span class="label">Check-out</span>
            <span class="value sh-dateval ph" id="shCoLabel">dd/mm/yyyy</span>
          </div>
          <div class="search-item guest-selector">
            <div class="guest-trigger" onclick="toggleGuests()">
              <span class="label">Guests</span>
              <span class="value" id="guestSummary">1 Adult</span>
            </div>
            <div class="guest-popup">
              <div class="guest-row">
                <div><strong>Adults</strong><br><small>Ages 18+</small></div>
                <div>
                  <button type="button" onclick="changeCount('adults',-1)">−</button>
                  <span id="adults">1</span>
                  <button type="button" onclick="changeCount('adults',1)">+</button>
                </div>
              </div>
              <hr>
              <div class="guest-row">
                <div><strong>Children</strong><br><small>Ages 0-13</small></div>
                <div>
                  <button type="button" onclick="changeCount('children',-1)">−</button>
                  <span id="children">0</span>
                  <button type="button" onclick="changeCount('children',1)">+</button>
                </div>
              </div>
            </div>
          </div>
          <div class="search-item sh-hours-field" onclick="shToggleHours(event)">
            <span class="label">Hours</span>
            <span class="value" id="shHoursLabel">Any</span>
            <input type="hidden" id="searchHours" value="">
            <div class="sh-hours-pop" id="shHoursPop" hidden>
              <div class="sh-hours-opt is-on" data-v="" onclick="shPickHours(this, event)">Any</div>
              <div class="sh-hours-opt" data-v="10" onclick="shPickHours(this, event)">10 hours</div>
              <div class="sh-hours-opt" data-v="21" onclick="shPickHours(this, event)">21 hours</div>
            </div>
          </div>
        </div>
        <button class="sh-cta" onclick="checkAvailability()">Check Availability</button>
        <div class="sh-cal" id="shCal" hidden>
          <div class="sh-cal-head">
            <button type="button" class="sh-cal-nav" id="shCalPrev" onclick="shCalNav(-1)">‹</button>
            <span id="shCalTitle"></span>
            <button type="button" class="sh-cal-nav" onclick="shCalNav(1)">›</button>
          </div>
          <div class="sh-cal-dow"><span>Su</span><span>Mo</span><span>Tu</span><span>We</span><span>Th</span><span>Fr</span><span>Sa</span></div>
          <div class="sh-cal-grid" id="shCalGrid"></div>
          <div class="sh-cal-foot"><span id="shCalHint">Select check-in date</span><button type="button" onclick="shCalClear()">Clear</button></div>
        </div>
      </div>

    </div>

    <div class="sh-hero-media">
      <img id="shHeroImg" src="" alt="Featured haven">
      <a class="sh-hero-tag" id="shHeroTag" href="#our-havens">
        <b id="shHeroName">Our Havens</b>
        <span id="shHeroPrice">Price starts at ₱999</span>
      </a>
    </div>

  </div>
</section>

<!-- availability search results render here (hides "Our Havens" once a search runs) -->
<section id="results"></section>

<section class="sh-section sh-wrap" id="our-havens">
  <div class="sh-sec-head">
    <div>
      <div class="sh-sec-eyebrow">Handpicked stays</div>
      <h2>Our Havens</h2>
    </div>
    <a class="sh-viewall" href="havens.html">View all havens →</a>
  </div>
  <div class="sh-cards" id="cards"></div>
</section>

<section class="sh-promo sh-wrap" id="promo">
  <div class="sh-promo-in">
    <div>
      <div class="sh-promo-eyebrow" id="shPromoEyebrow">Limited promo</div>
      <h2 id="shPromoTitle">Stay 3 nights, get the 4th on us — book direct this month.</h2>
    </div>
    <a class="sh-promo-btn" id="shPromoBtn" href="#our-havens">See all promos</a>
  </div>
</section>

<footer class="sh-footer" id="contact">
  <div class="sh-footer-in">
    <div class="sh-foot-brand">
      <a class="sh-brand" href="index.html"><span class="sh-badge"><img src="images/logo.png" alt="Staycation Haven PH"></span> Staycation Haven PH</a>
      <p id="shFootTagline">Mplace Tower D, Panay Ave, Quezon City. Comfortable, affordable and memorable staycations.</p>
    </div>
    <div class="sh-foot-col">
      <h4>Explore</h4>
      <a href="index.html">Home</a>
      <a href="havens.html">Havens</a>
      <a href="#promo">Promos</a>
      <a href="#contact">Contact</a>
      <a href="/be-a-partner">List your unit with us</a>
      <a href="/become-an-affiliate">Become an affiliate</a>
    </div>
    <div class="sh-foot-col">
      <h4>Get in touch</h4>
      <a href="#" id="shFootIg">Instagram</a>
      <a href="#" id="shFootFb">Facebook</a>
      <a href="#" id="shFootTiktok">TikTok</a>
      <a href="mailto:staycationhavenph@gmail.com" id="shFootEmail">staycationhavenph@gmail.com</a>
    </div>
  </div>
  <div class="sh-foot-bar">© 2026 Staycation Haven PH · All rights reserved</div>
</footer>

<script src="amenity-icons.js"></script>
<script src="havens-data.js"></script>
<script>
// Render the hero feature + haven cards from the shared data (managed in the Admin Dashboard)
(function renderHome(){
    function esc(s){ return String(s == null ? "" : s).replace(/[&<>"]/g, c => ({ "&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;" }[c])); }
    function cleanPrice(raw){ return String(raw || "").replace(/^\s*price\s*starts?\s*at:?\s*/i, "").trim(); }
    function metaOf(h){ return h.meta || h.type || h.roomType || (h.pax ? h.pax + " pax" : ""); }

    const havens = ((typeof loadHavens === "function") ? loadHavens() : []).filter(h => h.live !== false);

    // ---- editable site copy (dashboard → Website page → shph_settings.site) ----
    const site = (function(){ try{ return (JSON.parse(localStorage.getItem("shph_settings")) || {}).site || {}; }catch(e){ return {}; } })();
    const setText = (id, v) => { const el = document.getElementById(id); if(el && v != null && String(v).trim() !== "") el.textContent = String(v); };
    const setLink = (id, v) => { const el = document.getElementById(id); if(el && v != null && String(v).trim() !== "") el.href = String(v); };
    setText("shHeroEyebrow", site.heroEyebrow);
    setText("shHeroTitle", site.heroTitle);        // plain text (wraps naturally)
    setText("shHeroLead", site.heroSubtitle);
    setText("shPromoEyebrow", site.promoEyebrow);
    setText("shPromoTitle", site.promoTitle);
    setText("shPromoBtn", site.promoButton);
    setText("shFootTagline", site.footerTagline);
    if(site.footerEmail){ const em = document.getElementById("shFootEmail"); if(em){ em.textContent = site.footerEmail; em.href = "mailto:" + site.footerEmail; } }
    setLink("shFootIg", site.instagram);
    setLink("shFootFb", site.facebook);
    setLink("shFootTiktok", site.tiktok);

    // hero feature = the admin-chosen haven (site.heroHavenId), else the first live haven
    const feat = (site.heroHavenId && havens.find(h => String(h.id) === String(site.heroHavenId))) || havens[0];
    if(feat){
        const img = document.getElementById("shHeroImg");
        if(img){ img.src = feat.image || ""; img.alt = feat.name || "Featured haven"; }
        const nm = document.getElementById("shHeroName");
        const meta = metaOf(feat);
        if(nm) nm.textContent = feat.name + (meta ? " · " + meta : "");
        const pr = document.getElementById("shHeroPrice");
        if(pr){ const p = cleanPrice(feat.price); pr.textContent = p ? "Price starts at " + p : ""; }
        const tag = document.getElementById("shHeroTag");
        if(tag) tag.href = "havens.html?id=" + feat.id;
    }

    const cards = havens.slice(0, 3).map(h => {   // homepage shows only the first 3 (full list on Havens)
        const price = cleanPrice(h.price);
        const priceTxt = price ? "Price starts at " + price : "";
        const meta = metaOf(h);
        const desc = h.description || "";
        return `
        <article class="sh-card">
          <a class="sh-card-media" href="havens.html?id=${encodeURIComponent(h.id)}"><img src="${esc(h.image)}" alt="${esc(h.name)}" loading="lazy"></a>
          <div class="sh-card-body">
            <div class="sh-card-head">
              <h3>${esc(h.name)}</h3>
              ${meta ? `<span class="sh-card-meta">${esc(meta)}</span>` : ""}
            </div>
            ${desc ? `<p class="sh-card-desc">${esc(desc)}</p>` : ""}
            <div class="sh-card-foot">
              <span class="sh-card-price">${esc(priceTxt)}</span>
              <a class="sh-book" href="havens.html?id=${encodeURIComponent(h.id)}">Book</a>
            </div>
          </div>
        </article>`;
    }).join("");
    const wrap = document.getElementById("cards");
    if(wrap) wrap.innerHTML = cards;
})();
</script>
<!-- shared booking rules — MUST load before script.js, whose availability search calls SHB -->
<script src="booking-rules.js"></script>
<script src="script.js"></script>
<script>
/* ---------- homepage search: custom date-range calendar + hours dropdown ---------- */
(function(){
    let selStart = null, selEnd = null, viewDate = null;
    const cal = document.getElementById("shCal");
    const hoursPop = document.getElementById("shHoursPop");
    function iso(d){ return d.getFullYear() + "-" + String(d.getMonth()+1).padStart(2,"0") + "-" + String(d.getDate()).padStart(2,"0"); }
    function todayIso(){ return iso(new Date()); }
    function fmt(s){ if(!s) return "dd/mm/yyyy"; const d = new Date(s + "T00:00:00"); return String(d.getDate()).padStart(2,"0") + "/" + String(d.getMonth()+1).padStart(2,"0") + "/" + d.getFullYear(); }

    // booking flow reads dates through this (script.js / checkAvailability unchanged)
    window.getStayDates = function(){ return { start: selStart, end: selEnd }; };

    function closeOthers(){ if(hoursPop) hoursPop.hidden = true; const gp = document.querySelector(".guest-popup"); if(gp) gp.classList.remove("show"); }
    window.shOpenCal = function(which, e){
        if(e) e.stopPropagation();
        closeOthers();
        if(!viewDate){ const t = selStart ? new Date(selStart + "T00:00:00") : new Date(); viewDate = new Date(t.getFullYear(), t.getMonth(), 1); }
        renderCal(); cal.hidden = false;
    };
    window.openCalendar = function(){ window.shOpenCal("in"); };   // used by checkAvailability when no date
    window.shCalNav = function(delta){ const now = new Date(); const cand = new Date(viewDate.getFullYear(), viewDate.getMonth()+delta, 1); const min = new Date(now.getFullYear(), now.getMonth(), 1); if(cand < min) return; viewDate = cand; renderCal(); };
    window.shCalClear = function(){ selStart = null; selEnd = null; paint(); updateHours(); renderCal(); };
    window._shPick = function(s){
        if(!selStart || (selStart && selEnd)){ selStart = s; selEnd = null; }
        else if(s <= selStart){ selStart = s; selEnd = null; }
        else { selEnd = s; }
        paint(); updateHours(); renderCal();
        if(selStart && selEnd) setTimeout(function(){ cal.hidden = true; }, 160);
    };

    function renderCal(){
        const y = viewDate.getFullYear(), m = viewDate.getMonth();
        document.getElementById("shCalTitle").textContent = viewDate.toLocaleString("en-US", { month:"long", year:"numeric" });
        const now = new Date(); document.getElementById("shCalPrev").disabled = (y === now.getFullYear() && m === now.getMonth());
        const startDow = new Date(y, m, 1).getDay(), days = new Date(y, m+1, 0).getDate(), t = todayIso();
        let h = "";
        for(let i = 0; i < startDow; i++) h += '<span class="empty"></span>';
        for(let d = 1; d <= days; d++){
            const s = iso(new Date(y, m, d));
            if(s < t){ h += '<span class="past">' + d + '</span>'; continue; }
            let cls = "";
            if(s === selStart || s === selEnd) cls = "sel";
            else if(selStart && selEnd && s > selStart && s < selEnd) cls = "inrange";
            h += '<button type="button" class="' + cls + '" onclick="_shPick(\'' + s + '\')">' + d + '</button>';
        }
        document.getElementById("shCalGrid").innerHTML = h;
        document.getElementById("shCalHint").textContent = !selStart ? "Select check-in date" : (!selEnd ? "Select check-out date" : (fmt(selStart) + " → " + fmt(selEnd)));
    }
    function paint(){
        const ci = document.getElementById("shCiLabel"), co = document.getElementById("shCoLabel");
        ci.textContent = fmt(selStart); ci.classList.toggle("ph", !selStart);
        co.textContent = fmt(selEnd); co.classList.toggle("ph", !selEnd);
    }

    // ---- custom hours dropdown ----
    window.shToggleHours = function(e){ if(e) e.stopPropagation(); if(cal) cal.hidden = true; const gp = document.querySelector(".guest-popup"); if(gp) gp.classList.remove("show"); hoursPop.hidden = !hoursPop.hidden; };
    window.shPickHours = function(el, e){
        if(e) e.stopPropagation();
        document.getElementById("searchHours").value = el.dataset.v;
        document.getElementById("shHoursLabel").textContent = el.textContent.trim();
        hoursPop.querySelectorAll(".sh-hours-opt").forEach(function(o){ o.classList.toggle("is-on", o === el); });
        hoursPop.hidden = true;
        if(typeof refreshResultsIfShown === "function") refreshResultsIfShown();
    };

    // when the stay spans 2+ nights, set HOURS to "N nights" (adds it to the dropdown, selected)
    function updateHours(){
        const inp = document.getElementById("searchHours"), lab = document.getElementById("shHoursLabel");
        const ex = hoursPop.querySelector('[data-nights]'); if(ex) ex.remove();
        if(selStart && selEnd && selEnd > selStart){
            const nights = Math.round((new Date(selEnd) - new Date(selStart)) / 86400000);
            if(nights >= 2){
                inp.value = "nights:" + nights; lab.textContent = nights + " nights";
                const o = document.createElement("div"); o.className = "sh-hours-opt"; o.dataset.v = "nights:" + nights; o.setAttribute("data-nights", nights); o.textContent = nights + " nights"; o.onclick = function(e){ shPickHours(o, e); };
                hoursPop.insertBefore(o, hoursPop.firstChild);
                hoursPop.querySelectorAll(".sh-hours-opt").forEach(function(x){ x.classList.toggle("is-on", x === o); });
            }
        }
    }

    // close popups on an outside click
    document.addEventListener("click", function(e){
        if(cal && !cal.hidden && !cal.contains(e.target) && !e.target.closest(".sh-datefield")) cal.hidden = true;
        if(hoursPop && !hoursPop.hidden && !hoursPop.contains(e.target) && !e.target.closest(".sh-hours-field")) hoursPop.hidden = true;
    });
})();
</script>
<script>
/* ---------- mobile hamburger menu ---------- */
window.shToggleNav = function(e){ if(e) e.stopPropagation(); var l = document.getElementById("shLinks"); if(l) l.classList.toggle("show"); };
document.addEventListener("click", function(e){
    var l = document.getElementById("shLinks");
    if(!l || !l.classList.contains("show")) return;
    if(e.target.closest(".sh-burger")) return;   // the button's own toggle handles it
    l.classList.remove("show");                   // any other click (a link, or outside) closes it
});
</script>

</body>
</html>
